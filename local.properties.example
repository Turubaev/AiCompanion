## This file is an example of local.properties
# Copy this file to local.properties and add your actual values
# local.properties is already in .gitignore and will not be committed to git

# Location of the SDK. This is only used by Gradle.
sdk.dir=YOUR_SDK_PATH_HERE

# OpenAI API Key
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# MCP Server Configuration
# VPS: Tinkoff, Telegram, GitHub (обязательно)
MCP_SERVER_HOST=45.14.165.53
MCP_SERVER_PORT=8080

# Локальный MCP (эмулятор): если заданы — приложение подключается к обоим серверам,
# control_android_emulator вызывается на локальный, остальные инструменты — на VPS.
# Для эмулятора укажите 10.0.2.2 (адрес ПК с точки зрения эмулятора).
# MCP_SERVER_HOST_LOCAL=10.0.2.2
# MCP_SERVER_PORT_LOCAL=8080